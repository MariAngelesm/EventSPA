{
  "name": "final-spa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "json-server --watch database/db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "vite": "^7.0.4"
  },
  "dependencies": {
    "crypto-js": "^4.2.0"
  }
}
